Service bus project that ingests the ODL (Oracle Diagnostics Logging)
output and persists it into a database. 

Possible use-cases include support and audit

TODO: format / deserialise the XML payloads before persisting them. 